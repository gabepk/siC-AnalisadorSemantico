int condicao(int a, int b) {
    int z;
    int x;
    if (x < y) {
        z = x + y;
    }
    else {  // Comentario
        z = 2*x;  // Comentario
    }  // Comentario
    z = z.rmvfirst; // Comentario
    return z; // Comentario
}

int main () {
    queue<char> q;
    char i;
    int j;
    int k;
    i = 'a';
    j = 1;
    k = 0;
    
    e.setlast->x;
    
    if (i >= j) {
        k = condicao(i, j);
        i = 2 + 2;
    }
    return o;
} 
