
int var () {
    return q;
}

int main (int int) {

    x = var(); // Substituir por "x = var(a b c);" para gerar erro 2
    queue<char> q;
    int i0;
    int j1;
    float k;
    i = 10;
    j = 1;
    
    if (i >= j) {
	k = i + 0.5; // Substituir por "k = i + 0.5 +;" para gerar erro 3
    }
    
    while (i != 0) {
	q = q + 'a';
	i = i - 1;
    }
    
    return a;
}
