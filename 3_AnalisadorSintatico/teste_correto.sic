
int main () {
    queue<char> q;
    int i;
    int j;
    int k;
    i = '1';
    j = 1;
    k = 0;
    
    e.setlast->x;
    
    if (i >= j) {
    k = condicao(i, j);}
    return o;
} 
    


int condicao(int CUZIN, int CUZAO) {
    int z;
    if (x < y) {
        z = x + y;
    }
    else {  // Comentario
        z = 2*x;  // Comentario
    }  // Comentario
    z = z.rmvfirst; // Comentario
    return z; // Comentario
}
